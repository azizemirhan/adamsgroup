{
  "name": "adamsgroup",
  "version": "1.0.0",
  "private": true,
  "description": "Adams Group Web Sitesi",
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler deploy",
    "preview": "npx serve ./dist"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  },
  "engines": {
    "node": ">=18"
  }
}

